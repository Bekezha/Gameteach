# Copy to .env locally: copy .env.example .env (Windows) or cp .env.example .env
# CI uses defaults; GitHub secret DOTENV overrides full .env content.
API_URL=https://gameteach.onrender.com/api/users

